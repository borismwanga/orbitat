<div class="reservation-form">
  <%= simple_form_for [@spaceship, @reservation] do |f| %>
    <%= f.input :start_date, label: "From", as: :date, html5: true %>
    <%= f.input :end_date, label: "To", as: :date, html5: true %>
    <div class="reservation-submit">
      <%= f.submit %>
    </div>
  <% end %>
</div>
